name: flutter_app
description: flutter.deb test app.
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^7.0.1
  hotkey_manager: ^0.1.7
  tray_manager: ^0.2.0
  window_manager: ^0.1.5

flutter:
  uses-material-design: true
